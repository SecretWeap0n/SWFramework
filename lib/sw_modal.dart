import 'package:flutter/material.dart';

class SWModal extends StatelessWidget {
  const SWModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
